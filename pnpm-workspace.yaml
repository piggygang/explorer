allowBuilds:
  # Unlike dressme, sharp builds here: the explorer's core content will be
  # remote NFT images, which is exactly what next/image optimisation is for.
  sharp: true
  unrs-resolver: false
